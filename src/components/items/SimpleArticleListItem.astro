---
import type { CollectionEntry } from "astro:content";

interface Props {
  post: CollectionEntry<"blog">;
}

const {
  post: { data, id },
} = Astro.props;
---

<div class="flex gap-2.5">
  <time datetime={data.date.toISOString()} class="text-body/70">
    {new Date(data.date).toLocaleDateString("en-GB")}
  </time>

  <a href={"/posts/" + id} class="font-semibold">{data.title}</a>
</div>
